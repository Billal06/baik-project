# kode by Billal Fauzan dari bpk Haris Hasanudin
# Dibuat pada tanggal: 12 agustus 2020

# panggil fungsi
&pertambahan(10, 11)

hasil = &pengurangan(14, 1)
tulis "Hasil: ", hasil, "\n"

tamat # berfungsi untuk tidak mengulangi lagi

# set nama dan isi dari 'fungsi'
fungsi pertambahan(bil1, bil1) {
	tulis "Hasil: ", bil1 - bil2, "\n"
}

fungsi pengurangan(b1, b2) {
	hasil = b1 - b2
	balik hasil
}
